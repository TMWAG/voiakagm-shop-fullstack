import Modal from "@/components/modal";
import { links } from "../common/modalLinks";

export default function RegistrationModal(){
  return (
    <Modal links={links}>
      registration      
    </Modal>
  );
}